سند جامع نیازمندی‌های فنی و معماری دیتابیس «کارخانه هوش مصنوعی مجمع حکمت»
۱. چشم‌انداز راهبردی و فلسفه معماری سیستم
معماری این سیستم بر پایه فلسفه  «کارخانه کارخانه‌سازی»  استوار است. هدف نهایی، تولید یک محصول ایستا نیست، بلکه ما در حال طراحی  «نقشه‌های اتوکد»  و زیرساخت جاده‌ای هستیم که اجازه می‌دهد میراث علمی ۱۵ ساله مجمع، با هر تغییر در تکنولوژی (مانند ظهور Whisper v5 یا مدل‌های جدید Anthropic)، به سرعت و به صورت خودکار بازتولید شود. این رویکرد به معنای گذار از تولید دستی به یک  «خط تولید هوشمند»  است که در آن دیتابیس، مخزن پویای نقشه‌ها (Master Prompts) و متادیتای حاکم بر فرآیندهاست، نه صرفاً انبار داده‌های پردازش شده.تحلیل لایه «خب که چی؟» (ارزش استراتژیک):  جداسازی لایه «نقشه کارخانه» از «محصول نهایی»، ریسک منسوخ شدن تکنولوژی را از بین می‌برد. اهمیت این معماری در قابلیت  ارتقای خودکار کل دانش سازمان  است؛ به طوری که با معرفی یک مدل قوی‌تر، سیستم به صورت خودکار تمامی فایل‌های ۱۵ سال اخیر را در صف پردازش شبانه قرار داده و نسخه دقیق‌تر و غنی‌تری را جایگزین می‌کند. این یعنی سازمان همیشه در لبه تکنولوژی حرکت می‌کند، بدون آنکه هزینه‌ی انسانی مجددی برای پردازش داده‌های قدیمی بپردازد.اهداف کلان پروژه:
●	بازتولید خودکار و شبانه (Batch Processing):  قابلیت اجرای مجدد کل خط تولید برای هزاران رکورد بدون دخالت انسان.
●	ساختار جاده‌سازی (Scalability):  ایجاد مسیری هموار برای عبور مدل‌های مختلف هوش مصنوعی روی داده‌های سازمان.
●	انعطاف‌پذیری مدل‌محور:  قابلیت جایگزینی و مقایسه مدل‌های آنلاین (Claude, GPT) و آفلاین (DeepSeek) در هر لحظه.
●	صیانت از میراث علمی:  حفظ پیوند ناگسستنی بین داده‌های فرآوری شده و منبع اصلی برای جلوگیری از انحرافات علمی.تبدیل این دیدگاه به یک ساختار صلب مهندسی، مستلزم پیاده‌سازی منطق ریاضی «ضرب دکارتی» در هسته پایگاه داده است.
۲. منطق هسته دیتابیس: مفهوم ضرب دکارتی و پیوند (Traceability)
پایگاه داده ملزم است هر رکورد خروجی را به عنوان حاصل‌ضرب چندین بردار فنی ذخیره کند. خروجی نهایی صرفاً یک متن نیست؛ بلکه حاصل تقاطع «فایل ورودی»، «نوع عملیات»، «مدل هوش مصنوعی» و «نسخه مستر پرامپت» است.تحلیل لایه «خب که چی؟» (اهمیت پیوند):  حفظ لینک دقیق به فایل اصلی (Traceability) فراتر از یک نیاز فنی، یک الزام برای  اعتبار فقهی و علمی  است. در سیستم‌های RAG، بدون لینک مستقیم به ثانیه مشخص از صوت یا صفحه مشخص از سند، امکان  Hallucination Check (بررسی توهم مدل)  و تأییدیه انسانی غیرممکن است. هر خروجی ماشین باید شناسنامه‌ای داشته باشد که ثابت کند این مطلب دقیقاً از کدام منبع و در چه فرآیندی استخراج شده است.جدول منطق ذخیره‌سازی (ضرب دکارتی):| نوع فایل ورودی | اکشن اتوماسیون (Action) | مدل هوش مصنوعی (LLM/ASR) | نسخه مستر پرامپت | خروجی هدف (Output) || ------ | ------ | ------ | ------ | ------ || صوت (سخنرانی) | تبدیل به متن (STT) | Whisper v3 / v4 | Prompt-STT-v1.2 | متن با تگ زمانی (SRT) || ویدیو (نشست) | فهرست‌بندی یوتیوبی | Claude 3.5 Sonnet | Prompt-Chapter-v2.0 | فهرست موضوعی زمان‌دار || متن (کتاب/OCR) | خلاصه‌سازی اجتهادی | GPT-4o / DeepSeek | Prompt-Summary-v1.1 | ملخص مدیریتی و فنی || فایل Word | استخراج موضوعات | Anthropic Claude | Prompt-Topic-v3.0 | تگ‌های موضوعی غنی‌شده |
این منطق در لایه‌های پنج‌گانه زیر پیاده‌سازی می‌شود تا پایداری سیستم تضمین گردد.
۳. تشریح فنی لایه‌های پنج‌گانه زنجیره داده (Data Chain)
معماری دیتابیس باید به گونه‌ای باشد که جریان داده در این ۵ لایه را با حفظ یکپارچگی مدیریت کند.
۳.۱. لایه ۱: مدیریت ورودی، ذخیره‌سازی و متادیتای پویا
این لایه به عنوان «ورودی کارخانه» عمل می‌کند. دیتابیس باید توانایی پذیرش و دسته‌بندی انواع فایل (Excel, PDF, Voice, Video, Word) را داشته باشد.
●	الزامات متادیتا:  ذخیره فیلدهای شناسنامه‌ای شامل: استاد، موضوع، تاریخ دقیق، عنوان پروژه، فصل/جلسه، نوع سند (کتاب/مجله/سخنرانی) و وضعیت پردازش (مانند وضعیت OCR یا ASR).
●	منطق Chunking فنی:  معماری دیتابیس ملزم به پشتیبانی از منطق خردسازی فایل‌های طولانی است. هر قطعه (Chunk) باید دارای متادیتای مربوط به Token Limit و Overlap Percent باشد تا برای پردازش توسط مدل‌های با پنجره بافتی متفاوت، بهینه گردد.
۳.۲. لایه ۲: موتور اتوماسیون و مدیریت پرامپت
این لایه «نقشه ساخت» را مدیریت می‌کند و باید به ابزارهایی مانند Dify یا n8n متصل شود.
●	نسخه‌گذاری Git-like:  هر MasterPrompt باید در دیتابیس دارای تاریخچه نسخه و هش (Hash) باشد. هر خروجی باید به شناسه دقیق نسخه پرامپت لینک شود.
●	مدیریت صف (Queue Management):  سیستم باید برای پردازش‌های دسته‌ای (Batch) دارای جدول Jobs باشد که وضعیت هر وظیفه را مانیتور کند.
●	تخمین پیش از اجرا:  دیتابیس ملزم به داشتن فیلد EstimatedCost است تا قبل از شروع عملیات، هزینه توکن و زمان تخمینی بر اساس حجم داده محاسبه و به مدیر سیستم گزارش شود.
۳.۳. لایه ۳: کنترل کیفیت (QC) و بنچ‌مارک محتوایی
این بخش «واحد نظارت کارخانه» است که خروجی ماشین را با «نسخه طلایی انسانی» مقایسه می‌کند.
●	دیتابیس بنچ‌مارک:  ایجاد جدولی برای ذخیره HumanGroundTruth (داده‌های تایید شده توسط متخصص).
●	پروتکل ارزیابی:  سیستم باید خروجی جدید را پاراگراف‌به‌پاراگراف با نسخه شاخص مقایسه کرده و در فیلد QualityRate امتیازی بین ۰ تا ۱۰۰ ثبت کند. این شاخص مبنای تصمیم‌گیری برای ارتقای پرامپت‌ها خواهد بود.
۳.۴. لایه ۴: بردارسازی و مدیریت کالکشن (Vectorization)
در این لایه، خروجی‌های تایید شده توسط موتور Collection Maker به پایگاه داده‌های برداری (مانند ChromaDB) تزریق می‌شوند.
●	کالکشن‌های پویا:  سیستم باید بتواند بر اساس متادیتا (مثلاً: فقط دروس سال ۱۳۸۵)، کالکشن‌های موقت و تخصصی برای موتور RAG بسازد.
●	جستجوی هیبرید:  معماری باید همزمان از جستجوی لفظی (Lexical) و معنایی (Semantic) پشتیبانی کند تا دقت بازیابی در متون فقهی و تخصصی به حداکثر برسد.
۳.۵. لایه ۵: ارزیابی RAG و مدیریت انتشار (Release Management)
لایه نهایی، آماده‌سازی داده برای اپلیکیشن‌هایی مانند «چت‌تفسیر» یا «نور» است.
●	حلقه بازخورد (Feedback Loop):  ثبت دقیق لایک/دیس‌لایک‌های کاربران در جدول FeedbackLog و اتصال آن به جفتِ «پرامپت-مدل» جهت اصلاح خودکار رفتارهای سیستم در نسخه‌های بعدی.
۴. الزامات عملیاتی و تضمین کیفیت (QA)
برای پایداری کارخانه در پردازش‌های سنگین، رعایت الزامات زیر اجباری است:
1.	پایداری Batch:  سیستم باید برای پردازش‌های طولانی‌مدت (Long-running jobs) بهینه‌سازی شده و قابلیت Resume پس از قطع اتصال را داشته باشد.
2.	تست واحد (Unit Test):  قبل از هر ریلیز، سیستم باید «تست صحت پیوند» (Integrity of Links) را اجرا کند تا اطمینان حاصل شود که متون استخراج شده هنوز به لحظه دقیق در فایل صوتی یا صفحه PDF ارجاع می‌دهند.
3.	مانیتورینگ سخت‌افزاری:  ثبت مصرف منابع سیستم و هزینه‌های API به تفکیک هر پروژه در دیتابیس.
4.	عدم بازنویسی (Immutable Data):  سیستم نباید هیچ داده‌ای را Overwrite کند. برای نسخه‌های جدید باید از ساختار Versioning یا Soft Delete استفاده شود تا سوابق تولید دانش حفظ گردد.
۵. جمع‌بندی و نقشه راه پیاده‌سازی ERD
طراح دیتابیس باید این نیازمندی‌ها را به یک نمودار ERD تبدیل کند که در آن پویایی، تاریخچه‌نگاری و پیوند (Traceability) اولویت اول را دارند. رابط کاربری (UI) پیشنهادی باید مانند سیستم‌های "Mahd" یا "Fakoor" ساده و تمیز باشد، اما در پشت صحنه، پیچیدگی‌های ضرب دکارتی را مدیریت کند.انتیتی‌های کلیدی (Entities) الزامی در دیاگرام نهایی:
●	SourceFile:  ذخیره فایل خام، نوع (صوت/ویدیو/متن/پاورپوینت)، وضعیت OCR/ASR و متادیتای شناسنامه‌ای.
●	MasterPrompt:  مخزن پرامپت‌ها با ساختار درختی و نسخه‌گذاری Git-like.
●	AutomationFlow:  ذخیره متادیتای فلوهای طراحی شده در دیفای یا n8n.
●	AutomationJob:  ثبت جزئیات اجرا، وضعیت صف، هزینه تخمینی (EstimatedCost) و زمان اجرا.
●	ProcessedOutput:  خروجی‌های فرآوری شده (متن کامل، خلاصه، SRT، فهرست موضوعی) با لینک قطعی به منبع.
●	HumanGroundTruth:  داده‌های طلایی تایید شده توسط متخصص برای بنچ‌مارک.
●	BenchmarkResult:  نتایج مقایسه خودکار و امتیازهای کیفی (QualityRate).
●	VectorCollection:  نگاشت خروجی‌ها به کالکشن‌های برداری تخصصی.
●	FeedbackLog:  بازخوردهای انسانی لایه اپلیکیشن برای بهبود مستر پرامپت‌ها.ملاحظه نهایی:  اعتبار علمی این کارخانه در گرو «پیوند» (Link) است. هیچ داده‌ای نباید در دیتابیس ذخیره شود مگر آنکه مسیر بازگشت به فایل اصلی (حتی در سطح ثانیه یا صفحه) در آن ثبت شده باشد.

















متن کامل گفتگو


